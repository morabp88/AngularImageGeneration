import { InputstyleDirective } from './inputstyle.directive';

describe('InputstyleDirective', () => {
  it('should create an instance', () => {
    const directive = new InputstyleDirective();
    expect(directive).toBeTruthy();
  });
});
